# Modules can do 3 things
    ## they can give extra functionalities via the Python standard library
    ## it can help you organize your code via multiple files
    ## it allows you to import external modules


# Some modules we can import are from the Python Standard Library

# Searching for the right module in need is going to be something usual


import math
import random
import string

print(round(math.sqrt(4)))
print(random.random())